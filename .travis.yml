language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ddumitrescu/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ddumitrescu/docker-react npm run test
